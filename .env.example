# ===== ASTROTAROT — Frontend env =====
# URL gốc của backend REST (Spring Boot).
#
# Biến này được NHÚNG LÚC BUILD, không đọc lúc chạy. Đổi giá trị trên Vercel thì
# phải bấm Redeploy, refresh trang không ăn thua.
#
# Chạy ở máy:  http://localhost:8080
# Chạy thật:   https://api.ten-mien-cua-ban.com
#
# PHẢI là https khi FE đã lên Vercel. Trang https gọi API http bị trình duyệt
# chặn thẳng (mixed content) — trang vẫn hiện nhưng mọi thứ động đều chết.
VITE_API_BASE_URL=http://localhost:8080
